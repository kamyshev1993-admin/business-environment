package main.java.pages.modal;

import org.openqa.selenium.WebDriver;

public class UserRegistrationDialog extends AbstractModal {

    public UserRegistrationDialog(WebDriver driver) {
        super(driver, "userRegistrationDialog");
    }
}
